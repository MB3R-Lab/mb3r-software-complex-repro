schema_version: "1.2"
seed: 2607
trials: 300000
endpoint_weights:
  "frontend:GET /api/products": 0.25
  "frontend:GET /api/recommendations": 0.25
  "frontend:GET /api/cart": 0.25
  "frontend:POST /api/checkout": 0.25
profiles:
  - {name: p10, sampling_mode: fixed_k_service_set, failure_probability: 0.10, fixed_k_failures: 2}
  - {name: p30, sampling_mode: fixed_k_service_set, failure_probability: 0.30, fixed_k_failures: 4}
  - {name: p50, sampling_mode: fixed_k_service_set, failure_probability: 0.50, fixed_k_failures: 8}
  - {name: p70, sampling_mode: fixed_k_service_set, failure_probability: 0.70, fixed_k_failures: 10}
  - {name: p90, sampling_mode: fixed_k_service_set, failure_probability: 0.90, fixed_k_failures: 14}
gate:
  mode: report
  default_action: report
  global_threshold: 0.0
